# PyTorch CUDA 11.3 版本 wheel 的下载索引（pip 安装 torch==1.11.0+cu113 时所需）
--find-links https://download.pytorch.org/whl/torch_stable.html

# ===== 核心框架 =====
torch==1.11.0+cu113       # PyTorch 1.11（CUDA 11.3）：深度学习核心框架，模型/训练/数据各模块均依赖
torchdata==0.3.0          # PyTorch 数据管线（DataPipes）；当前自定义数据加载未直接使用，保留自原项目

# ===== 数据与 NLP =====
torchtext==0.12           # PyTorch NLP 库；当前已用自定义 Multi30k 加载器与词表替代其功能，仅保留兼容
spacy==3.2                # NLP 分词库；transformer_data.py 中加载德/英分词器（de_core_news_sm / en_core_web_sm）
pandas==1.3.5             # 数据分析库；当前模块未直接使用，保留自原始 notebook

# ===== 训练监控与实验追踪 =====
GPUtil                    # GPU 占用监控；transformer_training.py 中调用 GPUtil.showUtilization() 打印显存
wandb                     # Weights & Biases 实验追踪；README 列为主要依赖，用于记录训练指标

# ===== 可视化 =====
altair==4.1               # 声明式可视化库；当前模块未直接使用，保留自原始 notebook 的注意力可视化

# ===== 开发工具（非运行时依赖）=====
jupytext==1.13            # Jupyter notebook 与纯文本互转；拆分原 notebook 为 .py 模块时使用
flake8                    # Python 代码风格检查（Linter）
black                     # Python 代码格式化工具
